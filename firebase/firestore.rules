rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App interna: acceso total sin autenticación
    // Para producción real, restringir según necesidades
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
